# バージョン履歴

[English version](CHANGELOG.md)

## v0.8.1 (2026-08-08)

- **多行ページの OCR を高速化**しました。DEIM で複数行を検出した場合、これまでは各行を PARSeq で 1 行ずつ順番に認識していましたが、同時 4 行までの並列実行に変更 (worker-pool パターン)。新聞コラム、レシート、複数段落のドキュメントなど、行数が多いほど wall-clock 短縮を体感できます
- 出力順は上流の読み順解析 (evalPage / filterFurigana) を尊重するため、idx-based 書き込みで維持しています。1〜2行のケースでは影響なし
- **暗背景の Latin 短文で認識結果に `the the the ...`、`.ES.ES.ES ...`、`CHANGE CHAND CHAND ...` のような繰り返し・変異ノイズが混入する不具合を修正**。PARSeq が入力に確信を持てないとき CTC デコーダが loop / mutation loop を吐く挙動が原因。認識テキストに対して以下3種の検出を並行実行し、最も早い位置で切り捨てる (`trimCtcLoopTail`):
  1. 空白区切りトークンで3連続同一 (`the the the`)
  2. 長さ2〜10 文字の部分文字列が3回以上連続 (`.ES.ES.ES`)
  3. 同一 token 2連続 + その直前が prefix 3文字以上共有する短い token (`CHANGE CHAND CHAND` を trim して `CHANGELOG` 部分だけ残すなど)
  全経路 (小画像 bypass / DEIM ラインごと) に一律で適用
- **正当な繰り返しテキスト (`はい はい はい`, `ドドドドド`, `ありがとう ありがとう ありがとう` 等の chant / 擬音 / 強調) は保持**。3種の検出はいずれも、テキスト先頭 (pos 0) から始まる短いループ (テキスト全体が 30 文字未満) の場合は発火しないガードを持つ。CTC ループは通常 PARSeq の max seq を埋めて長くなるため、この長さヒューリスティックで区別できる
- 上記に加えて **暗背景の場合は PARSeq に渡す前に色反転** し、モデル学習分布 (白背景・黒文字) に寄せる処理を追加。GitHub dark theme のリンクチップなど、暗背景の中〜小サイズ Latin 文字列の認識精度が向上
- **DEIM が 0 検出だった場合と、検出はあったが全ラインで PARSeq が空文字を返した場合** の両方について、画像が中サイズ (短辺 ≤ 500px) に収まるなら小画像 bypass path にフォールバックする経路を追加。DEIM のスコア閾値では拾えないが PARSeq には読める暗チップ等をレスキュー
- **既知の限界**: underline 付きの大きめ暗背景リンクチップ (例: 幅 250px 超で `text-decoration: underline` 付き) では、underline のストロークが band 抽出時にテキスト行に混ざり込んで PARSeq が読めず、空文字が返ります。従来 (`the the the ...` のゴミ) より改善はしているが、正しい認識には underline 除去処理が必要 (別途対応予定)

## v0.8.0 (2026-08-06)

- **PDF ファイルの OCR に対応**しました。ポップアップの「PDFを開く」から、ローカルのパスワードなし PDF をビューアーで開いてページごとに OCR できます
- ビューアーには **ページナビゲーション**（◀ / ▶ ボタン、ページ番号表示、← / → キー）が追加され、各ページに対して既存の範囲選択・全体 OCR がそのまま動作します
- OCR 履歴には `filename.pdf#p2` の形式でページ番号を残すので、後から「どの PDF のどのページから取ったテキストか」を追跡できます
- パスワード付き PDF は現状サポート外で、明確なエラーメッセージで拒否します
- 内部実装: `pdfjs-dist` を同梱してすべてローカルで PDF を描画。**通信ゼロの方針は維持**しています
- 設計書: `docs/pdf-viewer-design.md`

## v0.7.1 (2026-07-09)

- 小さな範囲を選択して OCR したときに、**認識結果の末尾に「1」の繰り返しなど意図しない文字が混入する** ケースを修正しました。上下ぎりぎりに隣接行の文字断片が写り込むと、その断片が隣接色パディングで縦線として引き延ばされて、PARSeq が縦線を「1」として読んでしまうのが原因でした
- パディング色を **画像の枠全体から多数派の色を集計** する方式に変更し、断片ピクセルを 1 個引いてしまう問題を回避
- さらに、**行ごとのインク量プロファイル** から連続する ink 帯を検出し、最大の高さを持つ帯を本文行として抽出する処理を追加。上下の細い断片帯は自動的に弾かれるようになりました
- 詳細な経緯は [Zenn記事「Chrome拡張OCRの「小さな範囲選択」問題を、パディングのアルゴリズムを何度も直しながら解決してきた話」](https://zenn.dev/lecto/articles/small-selection-padding-journey) にまとめています

## v0.7.0 (2026-07-01)

- **OCR履歴** を追加しました。クリップボードにコピーした OCR 結果は自動的に PC 内 (`chrome.storage.local`) に保存され、後から履歴ページで再コピー・編集・削除できます。データは外部には一切送信されません
- 履歴ページ (`history.html`) では次のことができます:
  - テキスト・URL・ページタイトルでの検索
  - ワンクリックで再コピー
  - インラインでの編集（編集済みバッジ付き、「元に戻す」で編集前の内容へ復帰可能）
  - 個別削除・全削除、折り畳み表示
- ポップアップとオプションページの両方から「履歴を開く」ボタンでアクセス可能
- オプションページに「OCR履歴」セクションを追加し、履歴の有効/無効・最大件数 (10〜500) の設定と保存件数の確認、全削除ができます
- ビューアー (viewer) でも履歴保存が動作します

## v0.6.2 (2026-06-22)

- v0.6.1 で暫定的にロールバックしていた v0.6.0 の不具合を修正し、**ふりがな自動フィルタ・小さな選択範囲のOCR機能を再有効化** しました

## v0.6.1 (2026-06-22)

- v0.6.0 で報告された **短い英数字（小さなボタンの文字やコミットハッシュなど）をOCRすると、認識結果の末尾に「1」や「9」が大量に並ぶ不具合** への暫定対応として、v0.5.0 と同等の挙動にロールバック
- 例：`cfa8b33` を選択すると `cfa8b33 1999999999...` のように返ってきていた問題
- v0.6.2 で正式に修正しています

## v0.6.0 (2026-06-14)

- 漫画やルビ付き文章で、本文に隣接した **ふりがな（ルビ）を自動でフィルタリング** するようにしました。これまで結果が `いちばんかのうせい / 一番可能性を / も / 持ってるんだよ / げんや / 玄弥` のように本文とふりがなが混在していたのが、本文だけになります。本文より極端に小さい検出ボックスを検出 (DEIM) と認識 (PARSeq) の間で除外する仕組みなので、そもそも文字として読まれません
- **小さなボタンやリンクの文字など、短い選択範囲でも OCR が動く** ように。タイトなクロップ (短辺 200px 以下) では DEIM の検出スコアがしきい値を下回り、空文字が返るケースがありました。小さい画像では検出をスキップして、隣接色パディングを除去したクロップ全体を 1 ラインとしてアスペクト比を保ったまま PARSeq に渡します

## v0.5.0 (2026-05-27)

- 表・レシートのOCR結果を **タブ区切り** で出力。同じ行に検出された複数のセルが `\t` で連結され、Excel / Google Sheets にそのまま貼り付けると列が分かれます
- クリーニングルールの置換文字列で **エスケープ記法** をサポート。`\t` (タブ)、`\n` (改行)、`\r`、`\\`、`\0` が実文字として解釈されます（`$1` などの後方参照は従来通り）
- 範囲選択時の **青い選択枠がOCR画像に混入する不具合を修正**。`requestAnimationFrame` で再描画を待ってからキャプチャするよう変更。隣接色パディングが本来の色になり、検出精度も若干向上
- **デバッグモード** を追加（オプションページ > デバッグ）。有効にすると、OCRに渡される直前のクロップ画像（隣接色パディング込み）を保存・表示できます

## v0.4.0 (2026-05-17)

- 画像を右クリック →「ビューアーで画像を開く」を追加
- 小さい画像を拡大して範囲選択したいときや、ページ上でうまく選択できない画像でも、ビューアーで開いてOCRできます

## v0.3.0 (2026-04-29)

- 画像ビューアーページ追加（ポップアップ「画像を開く」「クリップボードの画像を開く」）
- ビューアーでファイル選択 / ドラッグ&ドロップ / Ctrl+V 貼り付けに対応
- ビューアーで「範囲選択を開始」「画像全体をOCR」を選択可能
- file:// の権限設定なしでローカル画像をOCR可能に
- clipboardRead 権限追加
- 設定（settings.ts）・クリーニングルールの共通モジュール化
- ビューアーでもアラート表示・クリーニングルールを反映
- ポップアップUIにセパレーター追加、ボタン色分け（青=OCR / グレー=開く）
- メッセージ送信を拡張ページ/通常ページで分岐（アラート重複修正）

## v0.2.0 (2026-04-27)

- タイトな範囲選択でもOCRが動くよう隣接色パディングを追加
- 短辺の30%（最大50px）を端ピクセル引き伸ばしで余白追加
- 白背景・暗背景どちらでも安定して検出可能に
- OCR統合テスト追加（onnxruntime-node + sharp）

## v0.1.0 (2026-04-19)

- 初回リリース
- 範囲選択OCR（ドラッグで囲んでクリップボードにコピー）
- DEIM（テキスト領域検出）+ PARSeq（文字認識）+ XY-Cut（読み順解析）
- 完全オフライン（通信ゼロ）
- 3通りの起動方法（ツールバー / Alt+Shift+O / 右クリックメニュー）
- 正規表現クリーニングルール（カンマ除去、スペース整理など）
- 日英UI自動切替
- モデルキャッシュ（IndexedDB）
